export interface ClickableElementProps {
  /**
   * Whether to use the default cursor instead of a pointer cursor.
   * @default false
   */
  isDefaultCursor?: boolean
  /**
   * Additional CSS classes to apply to the element.
   * @default ''
   */
  class?: string
}
