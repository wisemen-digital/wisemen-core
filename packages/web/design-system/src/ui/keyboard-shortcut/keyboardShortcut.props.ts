export interface KeyboardShortcutProps {
  /**
   * The keyboard shortcut string to display.
   * Use `_` to separate keys in a combination (e.g. "meta_k")
   * and `-` to separate keys in a sequence (e.g. "g-d").
   */
  keyboardShortcut: string
}
