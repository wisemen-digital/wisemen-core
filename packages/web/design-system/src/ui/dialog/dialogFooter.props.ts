export interface DialogFooterProps {
  /**
   * Whether to show the separator at the top of the footer.
   * @default true
   */
  hasSeparator?: boolean

  /**
   * @deprecated Use `hasSeparator` instead.
   */
  showSeparator?: boolean
}
