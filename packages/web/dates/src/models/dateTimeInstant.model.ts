import type { Temporal } from 'temporal-polyfill'

/**
 * A DateTimeInstant represents a single point in time independent of any calendar or time zone.
 */
export type DateTimeInstant = Temporal.Instant
