# Agent Notes

## Agent skills

### Domain docs

Per-package domain model: each package under `packages/*/*` owns its own `CONTEXT.md` glossary and `docs/adr/` decisions at its own root — there is no repo-wide `CONTEXT-MAP.md`. See `docs/agents/domain.md`.
