# Installation

Install the test utilities as a development dependency:

```bash
pnpm add -D @wisemen/vue-core-test-utils
```

```ts
import {
  MockResult,
  ServiceMock,
} from '@wisemen/vue-core-test-utils'
```
