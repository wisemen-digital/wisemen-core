---
"@wisemen/quantity": minor
---

Add calorie units to the `Energy` quantity: `CALORIE` (`cal`) and `KILOCALORIE` (`kcal`), using the thermochemical calorie (1 cal = 4.184 J). Joules were already supported.
