---
"formango": patch
---

Add `wisemen.skills` pointer to package.json and emit `skills/manifest.json` so `@wisemen/skills-cli` can index this package's bundled skills.

No source or runtime changes.
