{
  "name": "@wisemen/wisemen-core",
  "type": "module",
  "version": "0.0.0",
  "author": "Wisemen",
  "packageManager": "pnpm@11.4.0",
  "license": "SEE LICENSE IN LICENSE.md",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo watch dev",
    "test": "turbo run test",
    "repo:stats": "node --experimental-strip-types ./scripts/repoStats.ts",
    "lint:oxlint": "turbo run lint:oxlint",
    "lint:eslint": "turbo run lint:eslint",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "type-check": "turbo run type-check",
    "cleanup": "turbo run cleanup && rimraf .turbo dist coverage .eslintcache",
    "story:dev": "turbo watch story:dev",
    "story:build": "turbo run story:build --filter @wisemen/vue-core-design-system",
    "changeset:release": "pnpm build && changeset publish",
    "docs:dev": "turbo watch docs:dev",
    "docs:build": "turbo run docs:build",
    "docs:gen": "turbo run docs:gen",
    "docs:gen-eslint": "cd ./packages/web/eslint-config && pnpm exec eslint-config-inspector build --base /wisemen-core/eslint-inspector --outDir ../../../docs/public/eslint-inspector --config ./src/inspector.ts",
    "precommit": "turbo run lint type-check test",
    "ci": "turbo run build test lint type-check",
    "build-storybook": "turbo run story:build --filter @wisemen/vue-core-design-system"
  },
  "devDependencies": {
    "@commitlint/cli": "catalog:tooling",
    "@commitlint/config-conventional": "catalog:tooling",
    "@varlock/bumpy": "^1.12.0",
    "husky": "catalog:build",
    "rimraf": "catalog:build",
    "turbo": "catalog:build",
    "vitest": "catalog:test"
  }
}
